/**
 * Экран «Архив передач».
 *
 * Отдельный пункт меню нужен производству и отборке: посмотреть, что и когда ушло
 * в цех, и при необходимости вернуть материал в работу. Вся работа со списком
 * вынесена в общую панель, поэтому этот экран отвечает только за заголовок и
 * пояснение.
 */

import { ArchivePanel } from './ArchivePanel.js';

export function ArchivePage() {
  return (
    <div className="page">
      <div className="page-head">
        <div>
          <h1>Архив передач</h1>
          <div className="hint">
            Материалы, переданные производству. Запись — это снимок на момент передачи:
            она сохраняется, даже если спецификация изменится или исчезнет. Возврат в
            работу требует причины и фиксируется в журнале.
          </div>
        </div>
      </div>

      <ArchivePanel />
    </div>
  );
}
